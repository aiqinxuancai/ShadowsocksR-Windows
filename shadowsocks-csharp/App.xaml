<Application x:Class="Shadowsocks.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- WPF-UI Theme Resources -->
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />

                <!-- Application-wide styles and resources can be added here -->
            </ResourceDictionary.MergedDictionaries>

            <!-- Custom application-wide brushes and styles -->
            <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="LightGray" />

            <!-- TextBox style with disabled state -->
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{StaticResource DisabledBackgroundBrush}" />
                    </Trigger>
                    <Trigger Property="IsReadOnly" Value="True">
                        <Setter Property="Background" Value="{StaticResource DisabledBackgroundBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- BooleanToVisibilityConverter -->
            <BooleanToVisibilityConverter x:Key="Bool2Visibility" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
